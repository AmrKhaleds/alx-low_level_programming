#include <stdio.h>

/**
 * main - prints the phase with proper grammar,
 * but the outcome is a piece of art,
 * Return: 0 if exited properly, mpm-zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
